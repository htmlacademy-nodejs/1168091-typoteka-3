extends layout.pug

block content
  main
    - const email = user ? user?.email : ''
    h1.visually-hidden Регистрация пользователя
    section
      .popup.popup--registration.popup--anti
        a.popup__button.button.button--popup-close(aria-label='Закрыть окно' href='/')
          | Закрыть
          | окно
        h2.popup__title Войти
        .popup__form.form.form--log-in
          form(action='/login' method='post')
            .form__field
              label
                input(type='email' name='email' placeholder='Электронная почта' value=email)
            .form__field
              label
                input(type='password' name='password' placeholder='Пароль' required='')
            +insertErrorMessage(validationMessages)
            button.form__submit-btn.form__submit-btn--log-in.button.button--colored(type='submit') Войти
