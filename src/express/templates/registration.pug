extends layout.pug

block content
  main
  h1.visually-hidden Регистрация пользователя
  section
    .popup.popup--registration.popup--anti
      a.popup__button.button.button--popup-close(aria-label='Закрыть окно' href='/')
        | Закрыть
        | окно
      h2.popup__title Регистрация
      .popup__form.popup__form--active.form.form--register
        form(action='/register' method='POST' enctype='multipart/form-data')
          .form__field
            label
              input(type='email' name='email' placeholder='Электронная почта' value= dataForm ? dataForm.email : '')
          .form__field
            label
              input(type='text' name='first-name' placeholder='Имя' value= dataForm ? dataForm.firstName : '')
          .form__field
            label
              input(type='text' name='last-name' placeholder='Фамилия' value= dataForm ? dataForm.lastName : '')
          .form__field
            label
              input(type='password' name='password' placeholder='Пароль')
          .form__field
            label
              input(type='password' name='password-repeated' placeholder='Повтор пароля')
          if validationMessages
            .form__validation-error
              p.form__error-message При регистрации произошли ошибки:
                ul.form__errors
                  each message of validationMessages
                    li.form__error #{message}
          .form__image-loader
            a.form__avatar.avatar
              img(src='/img/icons/smile.svg' alt='аватар пользователя')
            label
              input.visually-hidden(type='file' name='avatar')
              | Загрузить фото профиля
          button.form__submit-btn.form__submit-btn--register.button.button--colored(type='submit') Зарегистрироваться
